#!/usr/bin/env python

def options(opt):
	pass

def configure(conf):
	pass

def build(bld):
	source = [
		'fakesteamapi.cpp',
		'../public/tier0/memoverride.cpp'
	]

	bld.shlib(
		target = 'steam_api',
		features = 'cxx',
		includes = '../public/steam',
		source = source,
		use = 'tier0',
		install_path = bld.env.LIBDIR,
		subsystem = bld.env.MSVC_SUBSYSTEM
	)
